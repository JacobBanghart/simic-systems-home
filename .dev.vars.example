# Copy to .dev.vars for local `astro dev` / `wrangler dev` — these populate
# the Cloudflare `env` binding at runtime and are also read as a fallback by
# the catalog scripts in scripts/ (see scripts/lib/env.mjs).
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
